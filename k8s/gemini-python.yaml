apiVersion: v1
kind: Pod
metadata:
  name: gemini-python
  labels:
    app: gemini-python
    tool: gemini-cli
    lang: python
spec:
  IMAGE_PULL_SECRET_BLOCK
  containers:
    - name: gemini-python
      image: REGISTRY/gemini-cli-python:IMAGE_TAG
      env:
        - name: GEMINI_API_KEY
          valueFrom:
            secretKeyRef:
              name: gemini-secret
              key: GEMINI_API_KEY
      stdin: true
      tty: true
      resources:
        requests:
          memory: "256Mi"
          cpu: "250m"
        limits:
          memory: "1Gi"
          cpu: "1000m"
  restartPolicy: Never
